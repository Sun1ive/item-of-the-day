export interface ProductTypes {
  title: string;
  src: string;
  icon: string;
}

export interface UserDataType {
  name: string;
  phone: number | null;
  email: string;
  address: string;
  additional: string;
}
